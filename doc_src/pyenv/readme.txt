
- if this directory is empty consider to build it using the `doc_src/make_env.sh` script.
- it is required to get in final HTTP server root dir as built in order to allow for CGI Python scriptz to run

- also consider to make executables (`chmod +x`) all `cgi-gin/*.py` recursively 

